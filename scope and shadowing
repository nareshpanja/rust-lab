fn main() {
    let x = 5;
    println!("Outside scope: x = {}", x);

    {
        let x = x + 5; // shadowing
        println!("Inside inner scope: x = {}", x);
    }

    println!("Outside again: x = {}", x);
}
